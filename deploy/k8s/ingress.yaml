apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reserveflow
  namespace: reserveflow
spec:
  rules:
    - host: reserveflow.local
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: backend-api
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
